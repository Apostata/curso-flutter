import 'dart:io';
import 'dart:typed_data';

File Unit8ListToFile(Uint8List webFile) {
  return File.fromRawPath(webFile);
}

// File Unit8ListToFile(Uint8List webFile) {
//   return File.fromRawPath(webFile);
// }
